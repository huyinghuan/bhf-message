{
  "name": "application-name",
  "version": "0.1.0",
  "scripts":{
    "test": "coffee --no-header -o ./chrome-extensions/js -c src/js && coffee --no-header -o ./chrome-extensions/js -c src/chrome"
  }
}
